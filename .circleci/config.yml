version: 2
jobs:
  test:
    docker:
      - image: circleci/python:2.7
    steps:
      - checkout
      - run: sudo apt-get install default-jdk
      - run: pip install -e .[test]
workflows:
  version: 2
  build_and_test:
    jobs:
      - test
