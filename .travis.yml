addons:
  apt:
    packages:
      - libfreetype6-dev
      - libhdf5-serial-dev
      - libpng-dev
      - libtiff5
      - libzmq3-dev
before_install:
  - pip install h5py libtiff matplotlib python-bioformats scipy zmq
cache:
  - apt
  - pip
install: pip freeze
language: python
python:
  - 2.7
