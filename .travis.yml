before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y python-h5py python-libtiff python-matplotlib
  - pip install python-bioformats
cache:
  apt: true
  directories:
    - ~/.cache/pip
install: pip freeze
language: python
notifications:
  email: false
python:
  - 2.7
virtualenv:
  system_site_packages: true
