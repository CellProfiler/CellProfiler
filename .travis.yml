before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y libhdf5-serial-dev libzmq3-dev
  # python-vigra python-wxgtk2.8
  # - pip install -e git+https://github.com/h5py/h5py.git#egg=h5py
  # - pip install -e git+https://github.com/CellH5/cellh5.git#egg=cellh5
cache:
  directories:
    - $HOME/.cache/pip
install:
  - pip install -U pip wheel
  - pip install -r requirements.txt
language: python
notifications:
  email: false
python:
  - 2.7
# script: python cpnose.py --noguitests --with-javabridge cellprofiler/tests/
script: echo "Hello, world!"
virtualenv:
  system_site_packages: true
