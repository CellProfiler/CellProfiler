before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y cython python-h5py python-libtiff python-matplotlib python-scipy python-wxgtk2.8
  - pip install python-bioformats pyzmq
cache:
  - pip
language: python
notifications:
  email: false
python:
  - 2.7
script: python cpnose.py --noguitests --nojavatests -m \(\?\:\^\)test_\(\?\!example\).\*
virtualenv:
  system_site_packages: true
