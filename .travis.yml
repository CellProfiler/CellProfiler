addons:
  apt:
    packages:
      - python-h5py
      - python-matplotlib
      - python-scipy
      - python-zmq
before_install:
  - pip install libtiff python-bioformats
install: pip freeze
language: python
python:
  - 2.7
