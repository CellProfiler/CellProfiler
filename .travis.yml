addons:
  apt:
    packages:
      - gfortran
      - libatlas-base-dev
      - libblas-dev
      - libfreetype6-dev
      - libhdf5-serial-dev
      - liblapack-dev
      - libpng-dev
      - libtiff5-dev
      - libzmq-dev
      - libzmq3-dev
      - python-h5py
      - python-libtiff
      - python-matplotlibs
      - python-pyzmq
before_install:
  - pip install python-bioformats
cache:
  apt: true
  directories:
    - ~/.cache/pip
install: pip freeze
language: python
notifications:
  email: false
python:
  - 2.7
virtualenv:
  system_site_packages: true
