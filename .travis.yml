addons:
  apt:
    packages:
      - gfortran
      - libatlas-base-dev
      - libblas-dev
      - libfreetype6-dev
      - libhdf5-dev
      - libhdf5-serial-dev
      - liblapack-dev
      - libpng-dev
      - libtiff5
      - libzmq-dev
      - libzmq3-dev
before_install:
  - pip install h5py
  - pip install libtiff
  - pip install matplotlib
  - pip install python-bioformats
  - pip install pyzmq
cache:
  apt: true
  directories:
    - ~/.cache/pip
install: pip freeze
language: python
notifications:
  email: false
python:
  - 2.7
sudo: false
