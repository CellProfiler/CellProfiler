before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y python-h5py python-libtiff python-matplotlib python-scipy python-wxgtk2.8
  - pip install cython python-bioformats pyzmq
install: python CellProfiler.py --build-and-exit
language: python
notifications:
  email: false
python:
  - 2.7
script: python cpnose.py --with-javabridge --noguitests -v --with-xunit -m \(\?\:\^\)test_\(\?\!example\).\*
virtualenv:
  system_site_packages: true
