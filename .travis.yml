before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y cython openjdk-7-jdk python-h5py python-matplotlib python-mysqldb python-scipy python-vigra python-wxgtk2.8 python-zmq
cache:
  pip: true
language: python
notifications:
  email: false
python:
  - 2.7
script:
virtualenv:
  system_site_packages: true
