before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y python-h5py python-libtiff python-matplotlib
  - pip install python-bioformats pyzmq
install: pip freeze
language: python
notifications:
  email: false
python:
  - 2.7
script: python CellProfiler.py --build-and-exit
virtualenv:
  system_site_packages: true
