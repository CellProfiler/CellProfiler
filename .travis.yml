before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y cython python-h5py python-libtiff python-matplotlib python-scipy python-wxgtk2.8
  - pip install pyzmq
  - python external_dependencies.py -o
cache:
  pip: true
language: python
notifications:
  email: false
python:
  - 2.7
script: python cpnose.py -v --noguitests --with-javabridge cellprofiler/tests
virtualenv:
  system_site_packages: true
