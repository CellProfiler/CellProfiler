addons:
  apt:
    packages:
      - python-wxgtk2.8
      - python-wxtools
      - wx2.8-doc
      - wx2.8-examples
      - wx2.8-headers
      - wx2.8-i18n
before_install:
  - pip install cython matplotlib numpy scipy
cache:
  pip: true
dist: trusty
language: python
notifications:
  email: false
python:
  - "2.7"
script:
  - py.test
sudo: required
