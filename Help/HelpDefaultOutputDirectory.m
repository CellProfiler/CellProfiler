function HelpDefaultOutputDirectory
helpdlg(help('HelpDefaultOutputDirectory'))

% Help for the default output directory:
% Select the default folder where you want CellProfiler's output to be
% saved. You will have the option to save output to other locations:
% for example, the output file can be saved elsewhere by typing a full
% pathname in the 'Name the output file' box, and processed images can
% be saved elsewhere by specifying an alternate pathname within the
% SaveImages module.